{
  "name": "waitstaff_calculator",
  "version": "1.0.0",
  "description": "waitstaff calculator",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "css:compile": "node-sass style.scss style.css",
    "watch": "chokidar style.scss -c 'npm run css:compile'"
  },
  "author": "Billy",
  "license": "ISC",
  "devDependencies": {
    "chokidar-cli": "^1.2.0",
    "node-sass": "^3.4.2"
  }
}
